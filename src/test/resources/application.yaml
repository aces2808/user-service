spring:
  application:
    name: user-service
  webflux:
    base-path: api
    data:
      mongodb:
        port: 27017
        database: userdb
de:
  flapdoodle:
    mongodb:
      embedded:
        version: "5.0.5"